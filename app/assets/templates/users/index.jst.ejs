<h1> Matches </h1>
<ul class='users'>
  <% users.each(function(user) { %>
    <li>
      <h3><%= user.escape('username') %></h3>
      <p><%= user.escape('language') %></p>
    </li>
  <% }) %>
</ul>
